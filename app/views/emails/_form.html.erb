<%= form_with(model: email, local: true) do |form| %>
  <% if email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
      <% email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sender %>
    <%= form.text_field :sender, id: :email_sender %>
  </div>

  <div class="field">
    <%= form.label :read %>
    <%= form.check_box :read, id: :email_read %>
  </div>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject, id: :email_subject %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, id: :email_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
